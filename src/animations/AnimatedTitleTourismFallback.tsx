import type { FC } from 'react';

export const AnimatedTitleTourismFallback: FC<{ isMobile: boolean }> = ({ }) => (
  <div className="text-3xl sm:text-5xl font-serif font-bold text-white">
    Turismo en San Juan Tahitic
  </div>
);
